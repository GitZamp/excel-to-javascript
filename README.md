# excel-to-javascript
A Python script for auto-filling web forms. 

It uses the strings from the first row of a sheet to retrieve the corresponding data and generate a block of JavaScript commands, which can then be pasted into the console
